{
  "name": "string-class",
  "version": "1.0.0",
  "description": "This class  extend the String class in Javascript. Using Prototype Oriented Programming, methods, procedures and instance attributes are added to the String class to support more string manipulation features.",
  "main": "string-class.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "browser-sync": "^2.18.5",
    "chai": "^3.5.0",
    "gulp": "^3.9.1",
    "mocha": "^3.2.0"
  },
  "devDependencies": {
    "coveralls": "^2.11.15",
    "istanbul": "^0.4.5",
    "mocha-lcov-reporter": "^1.2.0"
  },
  "scripts": {
    "test": "mocha test/string-class-test.js",
    "cover": "istanbul cover _mocha",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "author": "Olawale Aladeusi",
  "license": "ISC"
}
